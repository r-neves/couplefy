# Database Configuration
# Option 1: Individual connection parameters (local development only)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=couplefy_user
POSTGRES_PASSWORD=couplefy_password
POSTGRES_DB=couplefy_db

# Option 2: Connection string (RECOMMENDED for production/Supabase)
#
# IMPORTANT: Use Supabase's Connection Pooling URL for production!
#
# Get this from: Supabase Dashboard > Project Settings > Database > Connection string > Connection pooling
# - Uses port 6543 (pooler) instead of 5432 (direct)
# - Includes pgbouncer=true parameter
# - Uses "transaction" mode for optimal performance with Prisma
#
# Example format:
# DATABASE_URL=postgresql://postgres.xxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&pool_timeout=10
#
# Additional recommended parameters:
# - connection_limit=1: Each serverless function instance uses 1 connection
# - pool_timeout=10: Timeout for acquiring connection from pool (seconds)
#
# For Vercel/serverless deployments, this pooling URL is CRITICAL for performance
# DATABASE_URL=postgresql://postgres.xxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# Supabase Configuration (using new API key format)
# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OpenTelemetry Configuration (Optional - for performance monitoring)
#
# For local development with Jaeger (run `docker-compose up -d jaeger`):
# OTEL_ENABLED=true
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# Then view traces at: http://localhost:16686
#
# With Grafana Cloud:
# OTEL_ENABLED=true
# OTEL_TRACES_EXPORTER="otlp"
# OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-gb-south-1.grafana.net/otlp
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic <your-base64-encoded-credentials>
# OTEL_RESOURCE_ATTRIBUTES="service.name=xxxxxx,service.namespace=xxxxxx,deployment.environment=xxxxxx"
# OTEL_NODE_RESOURCE_DETECTORS="env,host,os" 